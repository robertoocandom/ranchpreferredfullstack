node_modules
dist
*.log

# SQLite dev database
prisma/dev.db
prisma/dev.db-journal

# Secrets — .env has working dev defaults and is fine to keep committed,
# but any local override should not be
.env.local
.vercel
